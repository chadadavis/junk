#!/usr/bin/env python
# CLI:
# python -c 'import sys;s=sys.argv[1]; a,r,z = s[0:1], s[1:-1], s[-1]; print(a+str(len(r))+z)' internationalization
import sys;s=sys.argv[1]; a,r,z = s[0:1], s[1:-1], s[-1]; print(a+str(len(r))+z)
